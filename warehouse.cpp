#include <iostream>#include <string>#include <queue>#include <map>#include <iterator>#include "warehouse.h"#include <vector>using namespace std;warehouse::warehouse(){}void warehouse::addWarehouse(string name){    wh[name] = items;  }void warehouse::addItem(std::string name, std::string upc){  // //TODO: If the name is not in the map make a new vector.   // vector<string> items;   // items.push_back(upc);  //   wh[name] = items;  // //TODO: Otherwise simply add the item to the list.  wh[name].push_back(upc);  cout << wh[name].at(0) << endl;}void warehouse::receive(string upc, int amount){}void warehouse::request(string upc, int amount){}